{% extends 'layout.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block pageContent %}
    <div class="container pt-3 pb-3 mb-3">
        <div class="row mb-3 ml-1 mr-1 justify-content-center">
            <div class="col-6">
                <div class="card border-secondary mb-3">
                    <div class="card-header">Formulaire de connexion</div>
                    <div class="card-body">
                        <form method="post">
                            {% if error %}
                                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                            {% endif %}

                            {% if app.user %}
                                <div class="mb-3">
                                    Vous êtes déjà connecté en tant que {{ app.user.username }}, <a href="{{ path('app_logout') }}">me déconnecter</a>
                                </div>
                            {% endif %}

                            <div class="form-group">
                                <label for="inputEmail">Email</label>
                                <input type="email" value="{{ last_username }}" name="email" id="inputEmail"
                                       class="form-control"
                                       required autofocus>
                            </div>
                            <div class="form-group">
                                <label for="inputPassword">Mot de passe</label>
                                <input type="password" name="password" id="inputPassword" class="form-control" required>
                            </div>

                            <div class="form-group">
                                <label for="remember_me">Se souvenir de moi</label>
                                <input type="checkbox" id="remember_me" name="_remember_me" checked/>
                            </div>

                            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                            <button class="btn btn-lg btn-primary form-control" type="submit">
                                <i class="fa fa-sign-in-alt"></i> Connecter
                            </button>

                            <p class="mt-3">
                                Pas encore inscrit ? <a href="{{ path('register') }}">c'est ici que ça se passe</a>
                            </p>
                            <p class="mt-3">
                                Mot de passe perdu ? <a href="{{ path('reset_password') }}">récupérer mon compte</a>
                            </p>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}